fastapi==0.104.1
uvicorn==0.24.0
redis>=4.2.0

# Audio
soundfile==0.12.1

# Coqui TTS - Upgraded for XTTSv2 support
TTS==0.22.0

# Torch CPU-only builds (much smaller than CUDA)
# Ensure these versions are compatible with the TTS version
torch==2.2.2+cpu
torchaudio==2.2.2+cpu
-f https://download.pytorch.org/whl/cpu/torch_stable.html
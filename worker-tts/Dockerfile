FROM python:3.11-slim

# System dependencies (for soundfile + numpy + TTS)
RUN apt-get update && apt-get install -y \
    git \
    ffmpeg \
    libsndfile1 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Install Python deps
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy app code
COPY . .

EXPOSE 8002
CMD ["uvicorn", "tts_worker:app", "--host", "0.0.0.0", "--port", "8002", "--reload"]
